# WebPanel - Advanced Script & File Management System

Современная веб-панель для управления скриптами и файлами с красивым интерфейсом в темных тонах с эффектом гласморфизма.

## 🌟 Возможности

### 📁 Управление файлами
- Загрузка файлов через drag & drop или обычным способом
- Автоматическая распаковка архивов (.zip, .rar) в отдельные проекты
- Просмотр структуры проектов
- Запуск скриптов прямо из интерфейса

### ⚙️ Управление процессами
- Мониторинг запущенных и завершенных скриптов
- Просмотр логов в реальном времени через WebSocket
- Принудительная остановка процессов
- История выполнения с возможностью удаления записей

### 💻 Встроенный терминал
- Выполнение произвольных команд
- Просмотр результатов выполнения
- Таймаут для длительных операций

### 🎨 Современный дизайн
- Темная тема с зелеными акцентами
- Эффект гласморфизма
- Адаптивный дизайн для мобильных устройств
- Плавные анимации и переходы

## 🚀 Быстрый старт

### Windows
1. Скачайте проект
2. Запустите `start.bat`
3. Откройте браузер по адресу `http://localhost:5000`

### Linux
1. Скачайте проект
2. Сделайте скрипт исполняемым: `chmod +x start.sh`
3. Запустите: `./start.sh`
4. Откройте браузер по адресу `http://localhost:5000`

## 📋 Требования

- Python 3.7+
- Современный веб-браузер
- RAR архиватор (для .rar файлов)

### Автоматическая установка зависимостей
Скрипты запуска автоматически установят все необходимые зависимости:
- Flask 2.3.2
- Flask-SocketIO 5.3.4
- psutil 5.9.5
- rarfile 4.0
- python-socketio 5.8.0
- eventlet 0.33.3

## 📖 Руководство пользователя

### Вкладка "Upload" (Загрузка)
- Перетащите файлы в область загрузки или нажмите для выбора
- Поддерживаемые форматы: .py, .js, .sh, .bat, .zip, .rar
- Архивы автоматически распаковываются в папку `projects/`

### Вкладка "Files" (Файлы)
- Просмотр загруженных файлов и проектов
- Кнопка "Run" для запуска скриптов
- Кнопка "Open" для просмотра содержимого проектов

### Вкладка "Processes" (Процессы)
- Список всех запущенных и завершенных процессов
- Статусы: `running`, `completed`, `failed`, `stopped`
- Действия:
  - **Logs** - просмотр логов (обновляются в реальном времени)
  - **Stop** - принудительная остановка (только для запущенных)
  - **Delete** - удаление записи из истории

### Вкладка "Terminal" (Терминал)
- Выполнение команд операционной системы
- Ввод команды и нажатие Enter или кнопки выполнения
- Просмотр вывода команды

## 🗂️ Структура проекта

```
webpanel/
├── app.py                 # Основной сервер Flask
├── requirements.txt       # Зависимости Python
├── start.bat             # Скрипт запуска для Windows
├── start.sh              # Скрипт запуска для Linux
├── README.md             # Документация
├── templates/
│   └── index.html        # HTML шаблон
├── static/
│   ├── css/
│   │   └── style.css     # Стили CSS
│   └── js/
│       └── app.js        # JavaScript логика
├── uploads/              # Загруженные файлы
├── projects/             # Распакованные проекты
└── logs/                 # Логи выполнения скриптов
```

## 🔧 Ручная установка

Если автоматические скрипты не работают:

1. Создайте виртуальное окружение:
   ```bash
   python -m venv venv
   ```

2. Активируйте его:
   - Windows: `venv\Scripts\activate`
   - Linux/macOS: `source venv/bin/activate`

3. Установите зависимости:
   ```bash
   pip install -r requirements.txt
   ```

4. Запустите сервер:
   ```bash
   python app.py
   ```

## 🛡️ Безопасность

- Сервер запускается локально на 127.0.0.1:5000
- Для продакшена измените настройки безопасности
- Будьте осторожны с выполнением команд в терминале

## 🔧 Настройка

Основные настройки в `app.py`:

```python
app.config['SECRET_KEY'] = 'your-secret-key-here'  # Измените в продакшене
app.config['UPLOAD_FOLDER'] = 'uploads'
app.config['PROJECTS_FOLDER'] = 'projects'
app.config['LOGS_FOLDER'] = 'logs'
```

## 🤝 Поддерживаемые форматы

### Скрипты
- **Python** (.py) - запуск через `python script.py`
- **JavaScript** (.js) - запуск через `node script.js`
- **Bash** (.sh) - запуск через `bash script.sh`
- **Batch** (.bat) - прямой запуск на Windows

### Архивы
- **ZIP** (.zip) - встроенная поддержка Python
- **RAR** (.rar) - требует установки RAR

## 📝 Логи

- Логи сохраняются в папке `logs/`
- Каждый процесс имеет уникальный ID
- Просмотр логов в реальном времени через WebSocket
- Автоматическое удаление логов при удалении процесса

## 🐛 Решение проблем

### Python не найден
- Убедитесь, что Python установлен и добавлен в PATH
- Windows: скачайте с python.org
- Linux: используйте пакетный менеджер

### Ошибки с RAR файлами
- Установите WinRAR (Windows) или unrar (Linux)
- Linux: `sudo apt install unrar`

### Порт 5000 занят
- Измените порт в app.py: `socketio.run(app, port=8080)`

## 📄 Лицензия

Этот проект создан для образовательных и личных целей.

## 🔄 Обновления

Версия 1.0.0:
- Базовый функционал
- Веб-интерфейс с гласморфизмом
- Управление процессами
- Встроенный терминал
- Поддержка архивов 